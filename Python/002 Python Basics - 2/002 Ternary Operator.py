# Ternaty Operator

# The format for ternary operator is:
# [Action when if is true] (if) (condition) (else)  [Action when else is true]

is_friend = True
can_message = 'Message Allowed' if is_friend else "Message is not Allowed"
print(can_message)
